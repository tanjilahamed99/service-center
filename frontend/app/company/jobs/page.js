import JobsListPage from "@/components/job/JobsListPage";

export default function AllJobsPage() {
  return (
    <JobsListPage
      variant="all"
      title="All Jobs"
      subtitle="Every job across every stage — filter by status, service center, engineer or call type."
    />
  );
}
